# ------- requirements.txt -------
# Runtime dependencies (must exist on PyPI as of 2025-06-02)
pyswisseph>=2.10.0,<2.11        # geocentric ephemeris
numpy>=1.26.0,<2.0              # numerics
pandas>=2.2.0,<3.0              # data frames
scipy>=1.13.0,<2.0              # scientific
scikit-learn>=1.5.0,<2.0        # ML core

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
redis>=5.0.0
slowapi>=0.1.0
structlog>=24.2.0

# optional build speed-ups (pre-built wheels)
# (none required – all above have manylinux wheels)
